import React from "react";
import preloader from "../../../icons/2.svg";

const Preloader = (props) => {
  return (
    <div>
      <img src={preloader} alt="" />
    </div>
  );
};

export default Preloader;
